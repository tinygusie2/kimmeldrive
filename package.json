{
  "name": "node-file-server",
  "version": "1.0.0",
  "description": "Simple file server with sharing",
  "main": "server.js",
  "scripts": {
    "start": "if [ ! -f .setup_complete ]; then bash setup.sh && touch .setup_complete && node server.js || exit 1; else node server.js; fi",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "file",
    "server",
    "express",
    "node"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",  
    "express": "^4.18.2",
    "fs-extra": "^11.1.1",
    "multer": "^1.4.5-lts.1",
    "qrcode": "^1.5.3",
    "uuid": "^9.0.1"
  }
}